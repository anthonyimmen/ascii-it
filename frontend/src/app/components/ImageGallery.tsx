'use client';

export default function ImageGallery() {
  return (
    <div
      style={{
        width: '100%',
        padding: '1rem 0.5rem 1.5rem',
        borderTop: '1px solid rgba(255, 255, 255, 0.1)',
        marginTop: 'auto',
        fontSize: '0.875rem',
        lineHeight: 1.6,
        color: 'var(--foreground)',
        opacity: 0.8,
      }}
    >
      cloud gallery and firebase login have been removed. generate ascii art, download it locally, and share however you like.
    </div>
  );
}
